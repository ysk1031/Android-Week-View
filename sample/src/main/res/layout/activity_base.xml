<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".BaseActivity"
    android:background="#fff">

    <com.alamkanak.weekview.WeekView
        android:id="@+id/weekView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:eventTextColor="@android:color/white"
        app:textSize="12sp"
        app:hourHeight="60dp"
        app:headerColumnTextColor="@color/toolbar_text"
        app:headerRowPadding="20dp"
        app:headerRowSeparator="8dp"
        app:headerRowSeparatorColor="@color/toolbar_text"
        app:headerRowSeparatorColorGravity="bottom"
        app:noOfVisibleDays="7"
        app:columnGap="0dp"
        app:headerRowBackgroundColor="@color/toolbar"
        app:dayBackgroundColor="#fff"
        app:todayBackgroundColor="#1848adff"
        app:headerColumnBackground="#ffffffff"
        app:daySeparatorColor="@color/accent"
        app:todayHeaderTextColor="@color/accent"
        app:showBorderEvent="true"
        app:borderEventColor="@color/event_color_border"
        app:borderEventThickness="5dp"
        app:enableZoom="false"/>

</RelativeLayout>
